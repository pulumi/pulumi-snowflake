# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs
from ._inputs import *

__all__ = [
    'GetOpenflowConnectorDefinitionsResult',
    'AwaitableGetOpenflowConnectorDefinitionsResult',
    'get_openflow_connector_definitions',
    'get_openflow_connector_definitions_output',
]

@pulumi.output_type
class GetOpenflowConnectorDefinitionsResult:
    """
    A collection of values returned by getOpenflowConnectorDefinitions.
    """
    def __init__(__self__, id=None, like=None, limit=None, openflow_connector_definitions=None):
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if like and not isinstance(like, str):
            raise TypeError("Expected argument 'like' to be a str")
        pulumi.set(__self__, "like", like)
        if limit and not isinstance(limit, dict):
            raise TypeError("Expected argument 'limit' to be a dict")
        pulumi.set(__self__, "limit", limit)
        if openflow_connector_definitions and not isinstance(openflow_connector_definitions, list):
            raise TypeError("Expected argument 'openflow_connector_definitions' to be a list")
        pulumi.set(__self__, "openflow_connector_definitions", openflow_connector_definitions)

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        The provider-assigned unique ID for this managed resource.
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter
    def like(self) -> Optional[_builtins.str]:
        """
        Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
        """
        return pulumi.get(self, "like")

    @_builtins.property
    @pulumi.getter
    def limit(self) -> Optional['outputs.GetOpenflowConnectorDefinitionsLimitResult']:
        """
        Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `starts_with` or `like`.
        """
        return pulumi.get(self, "limit")

    @_builtins.property
    @pulumi.getter(name="openflowConnectorDefinitions")
    def openflow_connector_definitions(self) -> Sequence['outputs.GetOpenflowConnectorDefinitionsOpenflowConnectorDefinitionResult']:
        """
        Holds the aggregated output of all Openflow connector definition queries.
        """
        return pulumi.get(self, "openflow_connector_definitions")


class AwaitableGetOpenflowConnectorDefinitionsResult(GetOpenflowConnectorDefinitionsResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetOpenflowConnectorDefinitionsResult(
            id=self.id,
            like=self.like,
            limit=self.limit,
            openflow_connector_definitions=self.openflow_connector_definitions)


def get_openflow_connector_definitions(like: Optional[_builtins.str] = None,
                                       limit: Optional[Union['GetOpenflowConnectorDefinitionsLimitArgs', 'GetOpenflowConnectorDefinitionsLimitArgsDict']] = None,
                                       opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetOpenflowConnectorDefinitionsResult:
    """
    > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `preview_features_enabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.

    Data source used to get details of filtered Openflow connector definitions, the Snowflake-managed templates a connector can be created from. Filtering is aligned with the current possibilities for [SHOW OPENFLOW CONNECTOR DEFINITIONS](https://docs.snowflake.com/en/sql-reference/sql/show-openflow-connector-definitions). Definitions are read-only, so there is no describe output and no matching resource.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_snowflake as snowflake

    # simple usage
    simple = snowflake.get_openflow_connector_definitions()
    # filtering (like)
    like = snowflake.get_openflow_connector_definitions(like="OPENFLOW_POSTGRES_CDC")
    # filtering (limit)
    limit = snowflake.get_openflow_connector_definitions(limit={
        "rows": 10,
        "from_": "OPENFLOW_",
    })
    ```

    > **Note** If a field has a default value, it is shown next to the type in the schema.


    :param _builtins.str like: Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
    :param Union['GetOpenflowConnectorDefinitionsLimitArgs', 'GetOpenflowConnectorDefinitionsLimitArgsDict'] limit: Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `starts_with` or `like`.
    """
    __args__ = dict()
    __args__['like'] = like
    __args__['limit'] = limit
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('snowflake:index/getOpenflowConnectorDefinitions:getOpenflowConnectorDefinitions', __args__, opts=opts, typ=GetOpenflowConnectorDefinitionsResult).value

    return AwaitableGetOpenflowConnectorDefinitionsResult(
        id=pulumi.get(__ret__, 'id'),
        like=pulumi.get(__ret__, 'like'),
        limit=pulumi.get(__ret__, 'limit'),
        openflow_connector_definitions=pulumi.get(__ret__, 'openflow_connector_definitions'))
def get_openflow_connector_definitions_output(like: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                                              limit: pulumi.Input[Optional[Optional[Union['GetOpenflowConnectorDefinitionsLimitArgs', 'GetOpenflowConnectorDefinitionsLimitArgsDict']]]] = None,
                                              opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetOpenflowConnectorDefinitionsResult]:
    """
    > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `preview_features_enabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.

    Data source used to get details of filtered Openflow connector definitions, the Snowflake-managed templates a connector can be created from. Filtering is aligned with the current possibilities for [SHOW OPENFLOW CONNECTOR DEFINITIONS](https://docs.snowflake.com/en/sql-reference/sql/show-openflow-connector-definitions). Definitions are read-only, so there is no describe output and no matching resource.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_snowflake as snowflake

    # simple usage
    simple = snowflake.get_openflow_connector_definitions()
    # filtering (like)
    like = snowflake.get_openflow_connector_definitions(like="OPENFLOW_POSTGRES_CDC")
    # filtering (limit)
    limit = snowflake.get_openflow_connector_definitions(limit={
        "rows": 10,
        "from_": "OPENFLOW_",
    })
    ```

    > **Note** If a field has a default value, it is shown next to the type in the schema.


    :param _builtins.str like: Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
    :param Union['GetOpenflowConnectorDefinitionsLimitArgs', 'GetOpenflowConnectorDefinitionsLimitArgsDict'] limit: Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `starts_with` or `like`.
    """
    __args__ = dict()
    __args__['like'] = like
    __args__['limit'] = limit
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('snowflake:index/getOpenflowConnectorDefinitions:getOpenflowConnectorDefinitions', __args__, opts=opts, typ=GetOpenflowConnectorDefinitionsResult)
    return __ret__.apply(lambda __response__: GetOpenflowConnectorDefinitionsResult(
        id=pulumi.get(__response__, 'id'),
        like=pulumi.get(__response__, 'like'),
        limit=pulumi.get(__response__, 'limit'),
        openflow_connector_definitions=pulumi.get(__response__, 'openflow_connector_definitions')))

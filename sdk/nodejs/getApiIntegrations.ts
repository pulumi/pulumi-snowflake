// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `previewFeaturesEnabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.
 *
 * Data source used to get details of filtered API integrations. Filtering is aligned with the current possibilities for [SHOW API INTEGRATIONS](https://docs.snowflake.com/en/sql-reference/sql/show-integrations) query (only `like` is supported). The results of SHOW and DESCRIBE are encapsulated in one output collection `apiIntegrations`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as snowflake from "@pulumi/snowflake";
 *
 * // Simple usage
 * const simple = snowflake.getApiIntegrations({});
 * export const simpleOutput = simple.then(simple => simple.apiIntegrations);
 * // Filtering (like)
 * const like = snowflake.getApiIntegrations({
 *     like: "api-integration-name",
 * });
 * export const likeOutput = like.then(like => like.apiIntegrations);
 * // Filtering by prefix (like)
 * const likePrefix = snowflake.getApiIntegrations({
 *     like: "prefix%",
 * });
 * export const likePrefixOutput = likePrefix.then(likePrefix => likePrefix.apiIntegrations);
 * // Without additional data (to limit the number of calls made for every found API integration)
 * const onlyShow = snowflake.getApiIntegrations({
 *     withDescribe: false,
 * });
 * export const onlyShowOutput = onlyShow.then(onlyShow => onlyShow.apiIntegrations);
 * ```
 *
 * > **Note** If a field has a default value, it is shown next to the type in the schema.
 */
export function getApiIntegrations(args?: GetApiIntegrationsArgs, opts?: pulumi.InvokeOptions): Promise<GetApiIntegrationsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("snowflake:index/getApiIntegrations:getApiIntegrations", {
        "like": args.like,
        "withDescribe": args.withDescribe,
    }, opts);
}

/**
 * A collection of arguments for invoking getApiIntegrations.
 */
export interface GetApiIntegrationsArgs {
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    like?: string;
    /**
     * (Default: `true`) Runs DESC API INTEGRATION for each integration returned by SHOW API INTEGRATIONS. The output of describe is saved to the describeOutput field. By default this value is set to true.
     */
    withDescribe?: boolean;
}

/**
 * A collection of values returned by getApiIntegrations.
 */
export interface GetApiIntegrationsResult {
    /**
     * Holds the aggregated output of all API integration details queries.
     */
    readonly apiIntegrations: outputs.GetApiIntegrationsApiIntegration[];
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    readonly like?: string;
    /**
     * (Default: `true`) Runs DESC API INTEGRATION for each integration returned by SHOW API INTEGRATIONS. The output of describe is saved to the describeOutput field. By default this value is set to true.
     */
    readonly withDescribe?: boolean;
}
/**
 * > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `previewFeaturesEnabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.
 *
 * Data source used to get details of filtered API integrations. Filtering is aligned with the current possibilities for [SHOW API INTEGRATIONS](https://docs.snowflake.com/en/sql-reference/sql/show-integrations) query (only `like` is supported). The results of SHOW and DESCRIBE are encapsulated in one output collection `apiIntegrations`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as snowflake from "@pulumi/snowflake";
 *
 * // Simple usage
 * const simple = snowflake.getApiIntegrations({});
 * export const simpleOutput = simple.then(simple => simple.apiIntegrations);
 * // Filtering (like)
 * const like = snowflake.getApiIntegrations({
 *     like: "api-integration-name",
 * });
 * export const likeOutput = like.then(like => like.apiIntegrations);
 * // Filtering by prefix (like)
 * const likePrefix = snowflake.getApiIntegrations({
 *     like: "prefix%",
 * });
 * export const likePrefixOutput = likePrefix.then(likePrefix => likePrefix.apiIntegrations);
 * // Without additional data (to limit the number of calls made for every found API integration)
 * const onlyShow = snowflake.getApiIntegrations({
 *     withDescribe: false,
 * });
 * export const onlyShowOutput = onlyShow.then(onlyShow => onlyShow.apiIntegrations);
 * ```
 *
 * > **Note** If a field has a default value, it is shown next to the type in the schema.
 */
export function getApiIntegrationsOutput(args?: GetApiIntegrationsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetApiIntegrationsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("snowflake:index/getApiIntegrations:getApiIntegrations", {
        "like": args.like,
        "withDescribe": args.withDescribe,
    }, opts);
}

/**
 * A collection of arguments for invoking getApiIntegrations.
 */
export interface GetApiIntegrationsOutputArgs {
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    like?: pulumi.Input<string | undefined>;
    /**
     * (Default: `true`) Runs DESC API INTEGRATION for each integration returned by SHOW API INTEGRATIONS. The output of describe is saved to the describeOutput field. By default this value is set to true.
     */
    withDescribe?: pulumi.Input<boolean | undefined>;
}

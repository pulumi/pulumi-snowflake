// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `previewFeaturesEnabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.
 *
 * Resource used to manage storage lifecycle policy objects. For more information, check [storage lifecycle policy documentation](https://docs.snowflake.com/en/sql-reference/sql/create-storage-lifecycle-policy).
 *
 * ## Example Usage
 *
 * > **Note** Instead of using fully_qualified_name, you can reference objects managed outside Terraform by constructing a correct ID, consult identifiers guide.
 * <!-- TODO(SNOW-1634854): include an example showing both methods-->
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as snowflake from "@pulumi/snowflake";
 *
 * //# Minimal
 * const basic = new snowflake.StorageLifecyclePolicy("basic", {
 *     database: "database_name",
 *     schema: "schema_name",
 *     name: "storage_lifecycle_policy_name",
 *     arguments: [{
 *         name: "VAL",
 *         type: "VARCHAR",
 *     }],
 *     body: "LENGTH(VAL) > 0",
 * });
 * //# Complete
 * const complete = new snowflake.StorageLifecyclePolicy("complete", {
 *     database: "database_name",
 *     schema: "schema_name",
 *     name: "storage_lifecycle_policy_name",
 *     arguments: [
 *         {
 *             name: "VAL",
 *             type: "VARCHAR",
 *         },
 *         {
 *             name: "CREATED_AT",
 *             type: "TIMESTAMP_NTZ",
 *         },
 *     ],
 *     body: "LENGTH(VAL) > 0",
 *     archiveTier: "COLD",
 *     archiveForDays: 365,
 *     comment: "My storage lifecycle policy",
 * });
 * ```
 *
 * > **Note** If a field has a default value, it is shown next to the type in the schema.
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import snowflake:index/storageLifecyclePolicy:StorageLifecyclePolicy example '"<database_name>"."<schema_name>"."<storage_lifecycle_policy_name>"'
 * ```
 */
export class StorageLifecyclePolicy extends pulumi.CustomResource {
    /**
     * Get an existing StorageLifecyclePolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: StorageLifecyclePolicyState, opts?: pulumi.CustomResourceOptions): StorageLifecyclePolicy {
        return new StorageLifecyclePolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'snowflake:index/storageLifecyclePolicy:StorageLifecyclePolicy';

    /**
     * Returns true if the given object is an instance of StorageLifecyclePolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is StorageLifecyclePolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === StorageLifecyclePolicy.__pulumiType;
    }

    /**
     * Specifies the number of days to keep rows that match the policy expression in archive storage. If set, Snowflake moves the data into archive storage according to the value you select for archive_tier. If unset, Snowflake expires the rows from the table without archiving the data.
     */
    declare public readonly archiveForDays: pulumi.Output<number | undefined>;
    /**
     * Specifies the type of storage tier to use for archiving rows. After you set the ARCHIVE_TIER for a policy, you can’t modify it. If you don’t specify this parameter, the policy is an expiration policy that deletes rows without archiving them. Valid values are (case-insensitive): `COOL` | `COLD`.
     */
    declare public readonly archiveTier: pulumi.Output<string | undefined>;
    /**
     * List of the arguments for the storage lifecycle policy. A signature specifies a set of attributes that must be considered to determine whether the row is ready for expiration.
     */
    declare public readonly arguments: pulumi.Output<outputs.StorageLifecyclePolicyArgument[]>;
    /**
     * Specifies the SQL expression. The expression can be any boolean-valued SQL expression. To mitigate permadiff on this field, the provider replaces blank characters with a space. This can lead to false positives in cases where a change in case or run of whitespace is semantically significant.
     */
    declare public readonly body: pulumi.Output<string>;
    /**
     * Specifies a comment for the storage lifecycle policy.
     */
    declare public readonly comment: pulumi.Output<string | undefined>;
    /**
     * The database in which to create the storage lifecycle policy. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    declare public readonly database: pulumi.Output<string>;
    /**
     * Outputs the result of `DESCRIBE STORAGE LIFECYCLE POLICY` for the given storage lifecycle policy.
     */
    declare public /*out*/ readonly describeOutputs: pulumi.Output<outputs.StorageLifecyclePolicyDescribeOutput[]>;
    /**
     * Fully qualified name of the resource. For more information, see [object name resolution](https://docs.snowflake.com/en/sql-reference/name-resolution).
     */
    declare public /*out*/ readonly fullyQualifiedName: pulumi.Output<string>;
    /**
     * Specifies the identifier for the storage lifecycle policy; must be unique for the database and schema in which the storage lifecycle policy is created. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The schema in which to create the storage lifecycle policy. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    declare public readonly schema: pulumi.Output<string>;
    /**
     * Outputs the result of `SHOW STORAGE LIFECYCLE POLICIES` for the given storage lifecycle policy.
     */
    declare public /*out*/ readonly showOutputs: pulumi.Output<outputs.StorageLifecyclePolicyShowOutput[]>;

    /**
     * Create a StorageLifecyclePolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: StorageLifecyclePolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: StorageLifecyclePolicyArgs | StorageLifecyclePolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as StorageLifecyclePolicyState | undefined;
            resourceInputs["archiveForDays"] = state?.archiveForDays;
            resourceInputs["archiveTier"] = state?.archiveTier;
            resourceInputs["arguments"] = state?.arguments;
            resourceInputs["body"] = state?.body;
            resourceInputs["comment"] = state?.comment;
            resourceInputs["database"] = state?.database;
            resourceInputs["describeOutputs"] = state?.describeOutputs;
            resourceInputs["fullyQualifiedName"] = state?.fullyQualifiedName;
            resourceInputs["name"] = state?.name;
            resourceInputs["schema"] = state?.schema;
            resourceInputs["showOutputs"] = state?.showOutputs;
        } else {
            const args = argsOrState as StorageLifecyclePolicyArgs | undefined;
            if (args?.arguments === undefined && !opts.urn) {
                throw new Error("Missing required property 'arguments'");
            }
            if (args?.body === undefined && !opts.urn) {
                throw new Error("Missing required property 'body'");
            }
            if (args?.database === undefined && !opts.urn) {
                throw new Error("Missing required property 'database'");
            }
            if (args?.schema === undefined && !opts.urn) {
                throw new Error("Missing required property 'schema'");
            }
            resourceInputs["archiveForDays"] = args?.archiveForDays;
            resourceInputs["archiveTier"] = args?.archiveTier;
            resourceInputs["arguments"] = args?.arguments;
            resourceInputs["body"] = args?.body;
            resourceInputs["comment"] = args?.comment;
            resourceInputs["database"] = args?.database;
            resourceInputs["name"] = args?.name;
            resourceInputs["schema"] = args?.schema;
            resourceInputs["describeOutputs"] = undefined /*out*/;
            resourceInputs["fullyQualifiedName"] = undefined /*out*/;
            resourceInputs["showOutputs"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(StorageLifecyclePolicy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering StorageLifecyclePolicy resources.
 */
export interface StorageLifecyclePolicyState {
    /**
     * Specifies the number of days to keep rows that match the policy expression in archive storage. If set, Snowflake moves the data into archive storage according to the value you select for archive_tier. If unset, Snowflake expires the rows from the table without archiving the data.
     */
    archiveForDays?: pulumi.Input<number | undefined>;
    /**
     * Specifies the type of storage tier to use for archiving rows. After you set the ARCHIVE_TIER for a policy, you can’t modify it. If you don’t specify this parameter, the policy is an expiration policy that deletes rows without archiving them. Valid values are (case-insensitive): `COOL` | `COLD`.
     */
    archiveTier?: pulumi.Input<string | undefined>;
    /**
     * List of the arguments for the storage lifecycle policy. A signature specifies a set of attributes that must be considered to determine whether the row is ready for expiration.
     */
    arguments?: pulumi.Input<pulumi.Input<inputs.StorageLifecyclePolicyArgument>[] | undefined>;
    /**
     * Specifies the SQL expression. The expression can be any boolean-valued SQL expression. To mitigate permadiff on this field, the provider replaces blank characters with a space. This can lead to false positives in cases where a change in case or run of whitespace is semantically significant.
     */
    body?: pulumi.Input<string | undefined>;
    /**
     * Specifies a comment for the storage lifecycle policy.
     */
    comment?: pulumi.Input<string | undefined>;
    /**
     * The database in which to create the storage lifecycle policy. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    database?: pulumi.Input<string | undefined>;
    /**
     * Outputs the result of `DESCRIBE STORAGE LIFECYCLE POLICY` for the given storage lifecycle policy.
     */
    describeOutputs?: pulumi.Input<pulumi.Input<inputs.StorageLifecyclePolicyDescribeOutput>[] | undefined>;
    /**
     * Fully qualified name of the resource. For more information, see [object name resolution](https://docs.snowflake.com/en/sql-reference/name-resolution).
     */
    fullyQualifiedName?: pulumi.Input<string | undefined>;
    /**
     * Specifies the identifier for the storage lifecycle policy; must be unique for the database and schema in which the storage lifecycle policy is created. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The schema in which to create the storage lifecycle policy. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    schema?: pulumi.Input<string | undefined>;
    /**
     * Outputs the result of `SHOW STORAGE LIFECYCLE POLICIES` for the given storage lifecycle policy.
     */
    showOutputs?: pulumi.Input<pulumi.Input<inputs.StorageLifecyclePolicyShowOutput>[] | undefined>;
}

/**
 * The set of arguments for constructing a StorageLifecyclePolicy resource.
 */
export interface StorageLifecyclePolicyArgs {
    /**
     * Specifies the number of days to keep rows that match the policy expression in archive storage. If set, Snowflake moves the data into archive storage according to the value you select for archive_tier. If unset, Snowflake expires the rows from the table without archiving the data.
     */
    archiveForDays?: pulumi.Input<number | undefined>;
    /**
     * Specifies the type of storage tier to use for archiving rows. After you set the ARCHIVE_TIER for a policy, you can’t modify it. If you don’t specify this parameter, the policy is an expiration policy that deletes rows without archiving them. Valid values are (case-insensitive): `COOL` | `COLD`.
     */
    archiveTier?: pulumi.Input<string | undefined>;
    /**
     * List of the arguments for the storage lifecycle policy. A signature specifies a set of attributes that must be considered to determine whether the row is ready for expiration.
     */
    arguments: pulumi.Input<pulumi.Input<inputs.StorageLifecyclePolicyArgument>[]>;
    /**
     * Specifies the SQL expression. The expression can be any boolean-valued SQL expression. To mitigate permadiff on this field, the provider replaces blank characters with a space. This can lead to false positives in cases where a change in case or run of whitespace is semantically significant.
     */
    body: pulumi.Input<string>;
    /**
     * Specifies a comment for the storage lifecycle policy.
     */
    comment?: pulumi.Input<string | undefined>;
    /**
     * The database in which to create the storage lifecycle policy. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    database: pulumi.Input<string>;
    /**
     * Specifies the identifier for the storage lifecycle policy; must be unique for the database and schema in which the storage lifecycle policy is created. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The schema in which to create the storage lifecycle policy. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    schema: pulumi.Input<string>;
}

// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `previewFeaturesEnabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.
 *
 * Data source used to get details of filtered Openflow deployments. Both Snowflake-managed and BYOC deployments are returned; the `type` field in `showOutput` distinguishes them. The results of SHOW and DESCRIBE are encapsulated in one output collection `openflowDeployments`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as snowflake from "@pulumi/snowflake";
 *
 * // simple usage
 * const simple = snowflake.getOpenflowDeployments({});
 * // filtering (like)
 * const like = snowflake.getOpenflowDeployments({
 *     like: "deployment-name",
 * });
 * // filtering (starts_with)
 * const startsWith = snowflake.getOpenflowDeployments({
 *     startsWith: "deployment-",
 * });
 * // filtering (limit)
 * const limit = snowflake.getOpenflowDeployments({
 *     limit: {
 *         rows: 10,
 *         from: "deployment-name",
 *     },
 * });
 * // without additional data (to limit the number of calls make sure to set all of these to false)
 * const onlyShow = snowflake.getOpenflowDeployments({
 *     withDescribe: false,
 *     withParameters: false,
 * });
 * ```
 *
 * > **Note** If a field has a default value, it is shown next to the type in the schema.
 */
export function getOpenflowDeployments(args?: GetOpenflowDeploymentsArgs, opts?: pulumi.InvokeOptions): Promise<GetOpenflowDeploymentsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("snowflake:index/getOpenflowDeployments:getOpenflowDeployments", {
        "like": args.like,
        "limit": args.limit,
        "startsWith": args.startsWith,
        "withDescribe": args.withDescribe,
        "withParameters": args.withParameters,
    }, opts);
}

/**
 * A collection of arguments for invoking getOpenflowDeployments.
 */
export interface GetOpenflowDeploymentsArgs {
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    like?: string;
    /**
     * Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `startsWith` or `like`.
     */
    limit?: inputs.GetOpenflowDeploymentsLimit;
    /**
     * Filters the output with **case-sensitive** characters indicating the beginning of the object name.
     */
    startsWith?: string;
    /**
     * (Default: `true`) Runs DESC OPENFLOW DEPLOYMENT for each deployment returned by SHOW OPENFLOW DEPLOYMENTS. The output of describe is saved to the description field. By default this value is set to true.
     */
    withDescribe?: boolean;
    /**
     * (Default: `true`) Runs SHOW PARAMETERS IN OPENFLOW DEPLOYMENT for each deployment returned by SHOW OPENFLOW DEPLOYMENTS. The output is saved to the parameters field. By default this value is set to true.
     */
    withParameters?: boolean;
}

/**
 * A collection of values returned by getOpenflowDeployments.
 */
export interface GetOpenflowDeploymentsResult {
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    readonly like?: string;
    /**
     * Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `startsWith` or `like`.
     */
    readonly limit?: outputs.GetOpenflowDeploymentsLimit;
    /**
     * Holds the aggregated output of all Openflow deployment details queries.
     */
    readonly openflowDeployments: outputs.GetOpenflowDeploymentsOpenflowDeployment[];
    /**
     * Filters the output with **case-sensitive** characters indicating the beginning of the object name.
     */
    readonly startsWith?: string;
    /**
     * (Default: `true`) Runs DESC OPENFLOW DEPLOYMENT for each deployment returned by SHOW OPENFLOW DEPLOYMENTS. The output of describe is saved to the description field. By default this value is set to true.
     */
    readonly withDescribe?: boolean;
    /**
     * (Default: `true`) Runs SHOW PARAMETERS IN OPENFLOW DEPLOYMENT for each deployment returned by SHOW OPENFLOW DEPLOYMENTS. The output is saved to the parameters field. By default this value is set to true.
     */
    readonly withParameters?: boolean;
}
/**
 * > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `previewFeaturesEnabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.
 *
 * Data source used to get details of filtered Openflow deployments. Both Snowflake-managed and BYOC deployments are returned; the `type` field in `showOutput` distinguishes them. The results of SHOW and DESCRIBE are encapsulated in one output collection `openflowDeployments`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as snowflake from "@pulumi/snowflake";
 *
 * // simple usage
 * const simple = snowflake.getOpenflowDeployments({});
 * // filtering (like)
 * const like = snowflake.getOpenflowDeployments({
 *     like: "deployment-name",
 * });
 * // filtering (starts_with)
 * const startsWith = snowflake.getOpenflowDeployments({
 *     startsWith: "deployment-",
 * });
 * // filtering (limit)
 * const limit = snowflake.getOpenflowDeployments({
 *     limit: {
 *         rows: 10,
 *         from: "deployment-name",
 *     },
 * });
 * // without additional data (to limit the number of calls make sure to set all of these to false)
 * const onlyShow = snowflake.getOpenflowDeployments({
 *     withDescribe: false,
 *     withParameters: false,
 * });
 * ```
 *
 * > **Note** If a field has a default value, it is shown next to the type in the schema.
 */
export function getOpenflowDeploymentsOutput(args?: GetOpenflowDeploymentsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetOpenflowDeploymentsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("snowflake:index/getOpenflowDeployments:getOpenflowDeployments", {
        "like": args.like,
        "limit": args.limit,
        "startsWith": args.startsWith,
        "withDescribe": args.withDescribe,
        "withParameters": args.withParameters,
    }, opts);
}

/**
 * A collection of arguments for invoking getOpenflowDeployments.
 */
export interface GetOpenflowDeploymentsOutputArgs {
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    like?: pulumi.Input<string | undefined>;
    /**
     * Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `startsWith` or `like`.
     */
    limit?: pulumi.Input<inputs.GetOpenflowDeploymentsLimitArgs | undefined>;
    /**
     * Filters the output with **case-sensitive** characters indicating the beginning of the object name.
     */
    startsWith?: pulumi.Input<string | undefined>;
    /**
     * (Default: `true`) Runs DESC OPENFLOW DEPLOYMENT for each deployment returned by SHOW OPENFLOW DEPLOYMENTS. The output of describe is saved to the description field. By default this value is set to true.
     */
    withDescribe?: pulumi.Input<boolean | undefined>;
    /**
     * (Default: `true`) Runs SHOW PARAMETERS IN OPENFLOW DEPLOYMENT for each deployment returned by SHOW OPENFLOW DEPLOYMENTS. The output is saved to the parameters field. By default this value is set to true.
     */
    withParameters?: pulumi.Input<boolean | undefined>;
}

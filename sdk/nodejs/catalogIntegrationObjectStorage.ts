// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * !> **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `previewFeaturesEnabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.
 *
 * Resource used to manage catalog integration objects for Apache Iceberg™ table files or Delta table files in object storage. For more information, check [catalog integration documentation](https://docs.snowflake.com/en/sql-reference/sql/create-catalog-integration-object-storage).
 *
 * ## Example Usage
 *
 * > **Note** Instead of using fully_qualified_name, you can reference objects managed outside Terraform by constructing a correct ID, consult identifiers guide.
 * <!-- TODO(SNOW-1634854): include an example showing both methods-->
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as snowflake from "@pulumi/snowflake";
 *
 * // basic resource
 * const basic = new snowflake.CatalogIntegrationObjectStorage("basic", {
 *     name: "example",
 *     enabled: false,
 *     tableFormat: "DELTA",
 * });
 * // complete resource
 * const complete = new snowflake.CatalogIntegrationObjectStorage("complete", {
 *     name: "example_complete",
 *     enabled: true,
 *     refreshIntervalSeconds: 60,
 *     comment: "Lorem ipsum",
 *     tableFormat: "ICEBERG",
 * });
 * ```
 *
 * > **Note** If a field has a default value, it is shown next to the type in the schema.
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import snowflake:index/catalogIntegrationObjectStorage:CatalogIntegrationObjectStorage example '"<name>"'
 * ```
 */
export class CatalogIntegrationObjectStorage extends pulumi.CustomResource {
    /**
     * Get an existing CatalogIntegrationObjectStorage resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: CatalogIntegrationObjectStorageState, opts?: pulumi.CustomResourceOptions): CatalogIntegrationObjectStorage {
        return new CatalogIntegrationObjectStorage(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'snowflake:index/catalogIntegrationObjectStorage:CatalogIntegrationObjectStorage';

    /**
     * Returns true if the given object is an instance of CatalogIntegrationObjectStorage.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is CatalogIntegrationObjectStorage {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === CatalogIntegrationObjectStorage.__pulumiType;
    }

    /**
     * (Default: ``) Specifies a comment for the catalog integration.
     */
    declare public readonly comment: pulumi.Output<string | undefined>;
    /**
     * Outputs the result of `DESCRIBE CATALOG INTEGRATION` for the given catalog integration.
     */
    declare public /*out*/ readonly describeOutputs: pulumi.Output<outputs.CatalogIntegrationObjectStorageDescribeOutput[]>;
    /**
     * Specifies whether the catalog integration is available for use for Iceberg tables. `true` allows users to create new Iceberg tables that reference this integration. Existing Iceberg tables that reference this integration function normally. `false` prevents users from creating new Iceberg tables that reference this integration. Existing Iceberg tables that reference this integration cannot access the catalog in the table definition.
     */
    declare public readonly enabled: pulumi.Output<boolean>;
    /**
     * Fully qualified name of the resource. For more information, see [object name resolution](https://docs.snowflake.com/en/sql-reference/name-resolution).
     */
    declare public /*out*/ readonly fullyQualifiedName: pulumi.Output<string>;
    /**
     * Specifies the identifier (i.e. name) of the catalog integration; must be unique in your account.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Specifies the number of seconds to wait between attempts to poll the external Iceberg catalog for metadata updates for automated refresh. For Delta-based tables, specifies the number of seconds to wait between attempts to poll your external cloud storage for new metadata.
     */
    declare public readonly refreshIntervalSeconds: pulumi.Output<number | undefined>;
    /**
     * Outputs the result of `SHOW CATALOG INTEGRATIONS` for the given catalog integration.
     */
    declare public /*out*/ readonly showOutputs: pulumi.Output<outputs.CatalogIntegrationObjectStorageShowOutput[]>;
    /**
     * Specifies the table format. Valid values are (case-insensitive): `ICEBERG` | `DELTA`.
     */
    declare public readonly tableFormat: pulumi.Output<string>;

    /**
     * Create a CatalogIntegrationObjectStorage resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: CatalogIntegrationObjectStorageArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: CatalogIntegrationObjectStorageArgs | CatalogIntegrationObjectStorageState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as CatalogIntegrationObjectStorageState | undefined;
            resourceInputs["comment"] = state?.comment;
            resourceInputs["describeOutputs"] = state?.describeOutputs;
            resourceInputs["enabled"] = state?.enabled;
            resourceInputs["fullyQualifiedName"] = state?.fullyQualifiedName;
            resourceInputs["name"] = state?.name;
            resourceInputs["refreshIntervalSeconds"] = state?.refreshIntervalSeconds;
            resourceInputs["showOutputs"] = state?.showOutputs;
            resourceInputs["tableFormat"] = state?.tableFormat;
        } else {
            const args = argsOrState as CatalogIntegrationObjectStorageArgs | undefined;
            if (args?.enabled === undefined && !opts.urn) {
                throw new Error("Missing required property 'enabled'");
            }
            if (args?.tableFormat === undefined && !opts.urn) {
                throw new Error("Missing required property 'tableFormat'");
            }
            resourceInputs["comment"] = args?.comment;
            resourceInputs["enabled"] = args?.enabled;
            resourceInputs["name"] = args?.name;
            resourceInputs["refreshIntervalSeconds"] = args?.refreshIntervalSeconds;
            resourceInputs["tableFormat"] = args?.tableFormat;
            resourceInputs["describeOutputs"] = undefined /*out*/;
            resourceInputs["fullyQualifiedName"] = undefined /*out*/;
            resourceInputs["showOutputs"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(CatalogIntegrationObjectStorage.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering CatalogIntegrationObjectStorage resources.
 */
export interface CatalogIntegrationObjectStorageState {
    /**
     * (Default: ``) Specifies a comment for the catalog integration.
     */
    comment?: pulumi.Input<string>;
    /**
     * Outputs the result of `DESCRIBE CATALOG INTEGRATION` for the given catalog integration.
     */
    describeOutputs?: pulumi.Input<pulumi.Input<inputs.CatalogIntegrationObjectStorageDescribeOutput>[]>;
    /**
     * Specifies whether the catalog integration is available for use for Iceberg tables. `true` allows users to create new Iceberg tables that reference this integration. Existing Iceberg tables that reference this integration function normally. `false` prevents users from creating new Iceberg tables that reference this integration. Existing Iceberg tables that reference this integration cannot access the catalog in the table definition.
     */
    enabled?: pulumi.Input<boolean>;
    /**
     * Fully qualified name of the resource. For more information, see [object name resolution](https://docs.snowflake.com/en/sql-reference/name-resolution).
     */
    fullyQualifiedName?: pulumi.Input<string>;
    /**
     * Specifies the identifier (i.e. name) of the catalog integration; must be unique in your account.
     */
    name?: pulumi.Input<string>;
    /**
     * Specifies the number of seconds to wait between attempts to poll the external Iceberg catalog for metadata updates for automated refresh. For Delta-based tables, specifies the number of seconds to wait between attempts to poll your external cloud storage for new metadata.
     */
    refreshIntervalSeconds?: pulumi.Input<number>;
    /**
     * Outputs the result of `SHOW CATALOG INTEGRATIONS` for the given catalog integration.
     */
    showOutputs?: pulumi.Input<pulumi.Input<inputs.CatalogIntegrationObjectStorageShowOutput>[]>;
    /**
     * Specifies the table format. Valid values are (case-insensitive): `ICEBERG` | `DELTA`.
     */
    tableFormat?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a CatalogIntegrationObjectStorage resource.
 */
export interface CatalogIntegrationObjectStorageArgs {
    /**
     * (Default: ``) Specifies a comment for the catalog integration.
     */
    comment?: pulumi.Input<string>;
    /**
     * Specifies whether the catalog integration is available for use for Iceberg tables. `true` allows users to create new Iceberg tables that reference this integration. Existing Iceberg tables that reference this integration function normally. `false` prevents users from creating new Iceberg tables that reference this integration. Existing Iceberg tables that reference this integration cannot access the catalog in the table definition.
     */
    enabled: pulumi.Input<boolean>;
    /**
     * Specifies the identifier (i.e. name) of the catalog integration; must be unique in your account.
     */
    name?: pulumi.Input<string>;
    /**
     * Specifies the number of seconds to wait between attempts to poll the external Iceberg catalog for metadata updates for automated refresh. For Delta-based tables, specifies the number of seconds to wait between attempts to poll your external cloud storage for new metadata.
     */
    refreshIntervalSeconds?: pulumi.Input<number>;
    /**
     * Specifies the table format. Valid values are (case-insensitive): `ICEBERG` | `DELTA`.
     */
    tableFormat: pulumi.Input<string>;
}

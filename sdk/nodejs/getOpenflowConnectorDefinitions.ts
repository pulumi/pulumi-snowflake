// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `previewFeaturesEnabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.
 *
 * Data source used to get details of filtered Openflow connector definitions, the Snowflake-managed templates a connector can be created from. Filtering is aligned with the current possibilities for [SHOW OPENFLOW CONNECTOR DEFINITIONS](https://docs.snowflake.com/en/sql-reference/sql/show-openflow-connector-definitions). Definitions are read-only, so there is no describe output and no matching resource.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as snowflake from "@pulumi/snowflake";
 *
 * // simple usage
 * const simple = snowflake.getOpenflowConnectorDefinitions({});
 * // filtering (like)
 * const like = snowflake.getOpenflowConnectorDefinitions({
 *     like: "OPENFLOW_POSTGRES_CDC",
 * });
 * // filtering (limit)
 * const limit = snowflake.getOpenflowConnectorDefinitions({
 *     limit: {
 *         rows: 10,
 *         from: "OPENFLOW_",
 *     },
 * });
 * ```
 *
 * > **Note** If a field has a default value, it is shown next to the type in the schema.
 */
export function getOpenflowConnectorDefinitions(args?: GetOpenflowConnectorDefinitionsArgs, opts?: pulumi.InvokeOptions): Promise<GetOpenflowConnectorDefinitionsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("snowflake:index/getOpenflowConnectorDefinitions:getOpenflowConnectorDefinitions", {
        "like": args.like,
        "limit": args.limit,
    }, opts);
}

/**
 * A collection of arguments for invoking getOpenflowConnectorDefinitions.
 */
export interface GetOpenflowConnectorDefinitionsArgs {
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    like?: string;
    /**
     * Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `startsWith` or `like`.
     */
    limit?: inputs.GetOpenflowConnectorDefinitionsLimit;
}

/**
 * A collection of values returned by getOpenflowConnectorDefinitions.
 */
export interface GetOpenflowConnectorDefinitionsResult {
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    readonly like?: string;
    /**
     * Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `startsWith` or `like`.
     */
    readonly limit?: outputs.GetOpenflowConnectorDefinitionsLimit;
    /**
     * Holds the aggregated output of all Openflow connector definition queries.
     */
    readonly openflowConnectorDefinitions: outputs.GetOpenflowConnectorDefinitionsOpenflowConnectorDefinition[];
}
/**
 * > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `previewFeaturesEnabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.
 *
 * Data source used to get details of filtered Openflow connector definitions, the Snowflake-managed templates a connector can be created from. Filtering is aligned with the current possibilities for [SHOW OPENFLOW CONNECTOR DEFINITIONS](https://docs.snowflake.com/en/sql-reference/sql/show-openflow-connector-definitions). Definitions are read-only, so there is no describe output and no matching resource.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as snowflake from "@pulumi/snowflake";
 *
 * // simple usage
 * const simple = snowflake.getOpenflowConnectorDefinitions({});
 * // filtering (like)
 * const like = snowflake.getOpenflowConnectorDefinitions({
 *     like: "OPENFLOW_POSTGRES_CDC",
 * });
 * // filtering (limit)
 * const limit = snowflake.getOpenflowConnectorDefinitions({
 *     limit: {
 *         rows: 10,
 *         from: "OPENFLOW_",
 *     },
 * });
 * ```
 *
 * > **Note** If a field has a default value, it is shown next to the type in the schema.
 */
export function getOpenflowConnectorDefinitionsOutput(args?: GetOpenflowConnectorDefinitionsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetOpenflowConnectorDefinitionsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("snowflake:index/getOpenflowConnectorDefinitions:getOpenflowConnectorDefinitions", {
        "like": args.like,
        "limit": args.limit,
    }, opts);
}

/**
 * A collection of arguments for invoking getOpenflowConnectorDefinitions.
 */
export interface GetOpenflowConnectorDefinitionsOutputArgs {
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    like?: pulumi.Input<string | undefined>;
    /**
     * Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `startsWith` or `like`.
     */
    limit?: pulumi.Input<inputs.GetOpenflowConnectorDefinitionsLimitArgs | undefined>;
}

// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `previewFeaturesEnabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.
 *
 * > **Note** `primaryKeyConstraint`, `uniqueConstraint`, and `foreignKeyConstraint` can only be set at creation time; changing or removing them recreates the whole table.
 *
 * Resource used to manage hybrid tables. For more information, check [hybrid tables documentation](https://docs.snowflake.com/en/sql-reference/sql/create-hybrid-table).
 *
 * ## Example Usage
 *
 * > **Note** Instead of using fully_qualified_name, you can reference objects managed outside Terraform by constructing a correct ID, consult identifiers guide.
 * <!-- TODO(SNOW-1634854): include an example showing both methods-->
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as snowflake from "@pulumi/snowflake";
 *
 * // basic resource
 * const basic = new snowflake.HybridTable("basic", {
 *     database: "DATABASE",
 *     schema: "SCHEMA",
 *     name: "HYBRID_TABLE",
 *     columns: [{
 *         name: "ID",
 *         type: "NUMBER(38,0)",
 *         notNull: true,
 *     }],
 *     primaryKeyConstraint: {
 *         columns: ["ID"],
 *     },
 * });
 * // complete resource
 * const complete = new snowflake.HybridTable("complete", {
 *     database: "DATABASE",
 *     schema: "SCHEMA",
 *     name: "HYBRID_TABLE",
 *     comment: "A hybrid table for HTAP workloads",
 *     dataRetentionTimeInDays: 7,
 *     maxDataExtensionTimeInDays: 14,
 *     columns: [
 *         {
 *             name: "ID",
 *             type: "NUMBER(38,0)",
 *             notNull: true,
 *         },
 *         {
 *             name: "NAME",
 *             type: "VARCHAR(256)",
 *             notNull: false,
 *             collate: "en-ci",
 *             comment: "Name column",
 *         },
 *         {
 *             name: "CREATED_AT",
 *             type: "TIMESTAMP_NTZ",
 *             notNull: true,
 *             "default": {
 *                 expression: "CURRENT_TIMESTAMP()",
 *             },
 *         },
 *         {
 *             name: "STATUS",
 *             type: "VARCHAR(256)",
 *             "default": {
 *                 constant: "ACTIVE",
 *             },
 *         },
 *         {
 *             name: "SEQ_ID",
 *             type: "NUMBER(38,0)",
 *             "default": {
 *                 sequence: "DATABASE.SCHEMA.MY_SEQUENCE",
 *             },
 *         },
 *         {
 *             name: "PARENT_ID",
 *             type: "NUMBER(38,0)",
 *             notNull: true,
 *         },
 *     ],
 *     primaryKeyConstraint: {
 *         name: "pk_hybrid_table",
 *         columns: ["ID"],
 *     },
 *     uniqueConstraints: [{
 *         name: "uq_name",
 *         columns: ["NAME"],
 *     }],
 *     foreignKeyConstraints: [{
 *         name: "fk_parent",
 *         columns: ["PARENT_ID"],
 *         tableName: "DATABASE.SCHEMA.PARENT_HYBRID_TABLE",
 *         refColumns: ["ID"],
 *     }],
 *     indices: [
 *         {
 *             name: "idx_name",
 *             columns: ["NAME"],
 *         },
 *         {
 *             name: "idx_name_created_at",
 *             columns: ["NAME"],
 *             includeColumns: ["CREATED_AT"],
 *         },
 *     ],
 * });
 * ```
 *
 * > **Note** If a field has a default value, it is shown next to the type in the schema.
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import snowflake:index/hybridTable:HybridTable example '"<db_name>"."<schema_name>"."<hybrid_table_name>"'
 * ```
 */
export class HybridTable extends pulumi.CustomResource {
    /**
     * Get an existing HybridTable resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: HybridTableState, opts?: pulumi.CustomResourceOptions): HybridTable {
        return new HybridTable(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'snowflake:index/hybridTable:HybridTable';

    /**
     * Returns true if the given object is an instance of HybridTable.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is HybridTable {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === HybridTable.__pulumiType;
    }

    /**
     * Definitions of a column to create in the hybrid table. Minimum one required.
     */
    declare public readonly columns: pulumi.Output<outputs.HybridTableColumn[]>;
    /**
     * Specifies a comment for the hybrid table.
     */
    declare public readonly comment: pulumi.Output<string | undefined>;
    /**
     * Specifies the retention period for the hybrid table so that Time Travel actions can be performed on historical data. For more information, check [DATA*RETENTION*TIME*IN*DAYS docs](https://docs.snowflake.com/en/sql-reference/parameters#data-retention-time-in-days).
     */
    declare public readonly dataRetentionTimeInDays: pulumi.Output<number>;
    /**
     * The database in which to create the hybrid table. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    declare public readonly database: pulumi.Output<string>;
    /**
     * Outputs the result of `DESCRIBE TABLE` for the given hybrid table.
     */
    declare public /*out*/ readonly describeOutputs: pulumi.Output<outputs.HybridTableDescribeOutput[]>;
    /**
     * Defines FOREIGN KEY constraints.
     */
    declare public readonly foreignKeyConstraints: pulumi.Output<outputs.HybridTableForeignKeyConstraint[] | undefined>;
    /**
     * Fully qualified name of the resource. For more information, see [object name resolution](https://docs.snowflake.com/en/sql-reference/name-resolution).
     */
    declare public /*out*/ readonly fullyQualifiedName: pulumi.Output<string>;
    /**
     * Defines secondary indexes on the hybrid table.
     */
    declare public readonly indices: pulumi.Output<outputs.HybridTableIndex[] | undefined>;
    /**
     * Object parameter that specifies the maximum number of days for which Snowflake can extend the data retention period for the hybrid table to prevent streams on it from becoming stale. For more information, check [MAX*DATA*EXTENSION*TIME*IN_DAYS docs](https://docs.snowflake.com/en/sql-reference/parameters#max-data-extension-time-in-days).
     */
    declare public readonly maxDataExtensionTimeInDays: pulumi.Output<number>;
    /**
     * Specifies the identifier for the hybrid table. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Defines the primary key constraint for the hybrid table.
     */
    declare public readonly primaryKeyConstraint: pulumi.Output<outputs.HybridTablePrimaryKeyConstraint>;
    /**
     * The schema in which to create the hybrid table. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    declare public readonly schema: pulumi.Output<string>;
    /**
     * Outputs the result of `SHOW PRIMARY KEYS`, `SHOW UNIQUE KEYS`, and `SHOW IMPORTED KEYS` for the given hybrid table, merged and grouped by constraint name and ordered by kind, then by column names. The `referencedTable`, `referencedColumns`, `deleteRule`, and `updateRule` fields are populated for FOREIGN KEY constraints only.
     */
    declare public /*out*/ readonly showKeysOutputs: pulumi.Output<outputs.HybridTableShowKeysOutput[]>;
    /**
     * Outputs the result of `SHOW HYBRID TABLES` for the given hybrid table.
     */
    declare public /*out*/ readonly showOutputs: pulumi.Output<outputs.HybridTableShowOutput[]>;
    /**
     * Defines UNIQUE constraints.
     */
    declare public readonly uniqueConstraints: pulumi.Output<outputs.HybridTableUniqueConstraint[] | undefined>;

    /**
     * Create a HybridTable resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: HybridTableArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: HybridTableArgs | HybridTableState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as HybridTableState | undefined;
            resourceInputs["columns"] = state?.columns;
            resourceInputs["comment"] = state?.comment;
            resourceInputs["dataRetentionTimeInDays"] = state?.dataRetentionTimeInDays;
            resourceInputs["database"] = state?.database;
            resourceInputs["describeOutputs"] = state?.describeOutputs;
            resourceInputs["foreignKeyConstraints"] = state?.foreignKeyConstraints;
            resourceInputs["fullyQualifiedName"] = state?.fullyQualifiedName;
            resourceInputs["indices"] = state?.indices;
            resourceInputs["maxDataExtensionTimeInDays"] = state?.maxDataExtensionTimeInDays;
            resourceInputs["name"] = state?.name;
            resourceInputs["primaryKeyConstraint"] = state?.primaryKeyConstraint;
            resourceInputs["schema"] = state?.schema;
            resourceInputs["showKeysOutputs"] = state?.showKeysOutputs;
            resourceInputs["showOutputs"] = state?.showOutputs;
            resourceInputs["uniqueConstraints"] = state?.uniqueConstraints;
        } else {
            const args = argsOrState as HybridTableArgs | undefined;
            if (args?.columns === undefined && !opts.urn) {
                throw new Error("Missing required property 'columns'");
            }
            if (args?.database === undefined && !opts.urn) {
                throw new Error("Missing required property 'database'");
            }
            if (args?.primaryKeyConstraint === undefined && !opts.urn) {
                throw new Error("Missing required property 'primaryKeyConstraint'");
            }
            if (args?.schema === undefined && !opts.urn) {
                throw new Error("Missing required property 'schema'");
            }
            resourceInputs["columns"] = args?.columns;
            resourceInputs["comment"] = args?.comment;
            resourceInputs["dataRetentionTimeInDays"] = args?.dataRetentionTimeInDays;
            resourceInputs["database"] = args?.database;
            resourceInputs["foreignKeyConstraints"] = args?.foreignKeyConstraints;
            resourceInputs["indices"] = args?.indices;
            resourceInputs["maxDataExtensionTimeInDays"] = args?.maxDataExtensionTimeInDays;
            resourceInputs["name"] = args?.name;
            resourceInputs["primaryKeyConstraint"] = args?.primaryKeyConstraint;
            resourceInputs["schema"] = args?.schema;
            resourceInputs["uniqueConstraints"] = args?.uniqueConstraints;
            resourceInputs["describeOutputs"] = undefined /*out*/;
            resourceInputs["fullyQualifiedName"] = undefined /*out*/;
            resourceInputs["showKeysOutputs"] = undefined /*out*/;
            resourceInputs["showOutputs"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(HybridTable.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering HybridTable resources.
 */
export interface HybridTableState {
    /**
     * Definitions of a column to create in the hybrid table. Minimum one required.
     */
    columns?: pulumi.Input<pulumi.Input<inputs.HybridTableColumn>[] | undefined>;
    /**
     * Specifies a comment for the hybrid table.
     */
    comment?: pulumi.Input<string | undefined>;
    /**
     * Specifies the retention period for the hybrid table so that Time Travel actions can be performed on historical data. For more information, check [DATA*RETENTION*TIME*IN*DAYS docs](https://docs.snowflake.com/en/sql-reference/parameters#data-retention-time-in-days).
     */
    dataRetentionTimeInDays?: pulumi.Input<number | undefined>;
    /**
     * The database in which to create the hybrid table. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    database?: pulumi.Input<string | undefined>;
    /**
     * Outputs the result of `DESCRIBE TABLE` for the given hybrid table.
     */
    describeOutputs?: pulumi.Input<pulumi.Input<inputs.HybridTableDescribeOutput>[] | undefined>;
    /**
     * Defines FOREIGN KEY constraints.
     */
    foreignKeyConstraints?: pulumi.Input<pulumi.Input<inputs.HybridTableForeignKeyConstraint>[] | undefined>;
    /**
     * Fully qualified name of the resource. For more information, see [object name resolution](https://docs.snowflake.com/en/sql-reference/name-resolution).
     */
    fullyQualifiedName?: pulumi.Input<string | undefined>;
    /**
     * Defines secondary indexes on the hybrid table.
     */
    indices?: pulumi.Input<pulumi.Input<inputs.HybridTableIndex>[] | undefined>;
    /**
     * Object parameter that specifies the maximum number of days for which Snowflake can extend the data retention period for the hybrid table to prevent streams on it from becoming stale. For more information, check [MAX*DATA*EXTENSION*TIME*IN_DAYS docs](https://docs.snowflake.com/en/sql-reference/parameters#max-data-extension-time-in-days).
     */
    maxDataExtensionTimeInDays?: pulumi.Input<number | undefined>;
    /**
     * Specifies the identifier for the hybrid table. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Defines the primary key constraint for the hybrid table.
     */
    primaryKeyConstraint?: pulumi.Input<inputs.HybridTablePrimaryKeyConstraint | undefined>;
    /**
     * The schema in which to create the hybrid table. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    schema?: pulumi.Input<string | undefined>;
    /**
     * Outputs the result of `SHOW PRIMARY KEYS`, `SHOW UNIQUE KEYS`, and `SHOW IMPORTED KEYS` for the given hybrid table, merged and grouped by constraint name and ordered by kind, then by column names. The `referencedTable`, `referencedColumns`, `deleteRule`, and `updateRule` fields are populated for FOREIGN KEY constraints only.
     */
    showKeysOutputs?: pulumi.Input<pulumi.Input<inputs.HybridTableShowKeysOutput>[] | undefined>;
    /**
     * Outputs the result of `SHOW HYBRID TABLES` for the given hybrid table.
     */
    showOutputs?: pulumi.Input<pulumi.Input<inputs.HybridTableShowOutput>[] | undefined>;
    /**
     * Defines UNIQUE constraints.
     */
    uniqueConstraints?: pulumi.Input<pulumi.Input<inputs.HybridTableUniqueConstraint>[] | undefined>;
}

/**
 * The set of arguments for constructing a HybridTable resource.
 */
export interface HybridTableArgs {
    /**
     * Definitions of a column to create in the hybrid table. Minimum one required.
     */
    columns: pulumi.Input<pulumi.Input<inputs.HybridTableColumn>[]>;
    /**
     * Specifies a comment for the hybrid table.
     */
    comment?: pulumi.Input<string | undefined>;
    /**
     * Specifies the retention period for the hybrid table so that Time Travel actions can be performed on historical data. For more information, check [DATA*RETENTION*TIME*IN*DAYS docs](https://docs.snowflake.com/en/sql-reference/parameters#data-retention-time-in-days).
     */
    dataRetentionTimeInDays?: pulumi.Input<number | undefined>;
    /**
     * The database in which to create the hybrid table. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    database: pulumi.Input<string>;
    /**
     * Defines FOREIGN KEY constraints.
     */
    foreignKeyConstraints?: pulumi.Input<pulumi.Input<inputs.HybridTableForeignKeyConstraint>[] | undefined>;
    /**
     * Defines secondary indexes on the hybrid table.
     */
    indices?: pulumi.Input<pulumi.Input<inputs.HybridTableIndex>[] | undefined>;
    /**
     * Object parameter that specifies the maximum number of days for which Snowflake can extend the data retention period for the hybrid table to prevent streams on it from becoming stale. For more information, check [MAX*DATA*EXTENSION*TIME*IN_DAYS docs](https://docs.snowflake.com/en/sql-reference/parameters#max-data-extension-time-in-days).
     */
    maxDataExtensionTimeInDays?: pulumi.Input<number | undefined>;
    /**
     * Specifies the identifier for the hybrid table. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Defines the primary key constraint for the hybrid table.
     */
    primaryKeyConstraint: pulumi.Input<inputs.HybridTablePrimaryKeyConstraint>;
    /**
     * The schema in which to create the hybrid table. Due to technical limitations (read more here), avoid using the following characters: `|`, `.`, `"`.
     */
    schema: pulumi.Input<string>;
    /**
     * Defines UNIQUE constraints.
     */
    uniqueConstraints?: pulumi.Input<pulumi.Input<inputs.HybridTableUniqueConstraint>[] | undefined>;
}

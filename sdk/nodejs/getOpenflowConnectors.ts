// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `previewFeaturesEnabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.
 *
 * Data source used to get details of filtered Openflow connectors. Filtering is aligned with the current possibilities for [SHOW OPENFLOW CONNECTORS](https://docs.snowflake.com/en/sql-reference/sql/show-openflow-connectors). The results of SHOW and DESCRIBE are encapsulated in one output collection `openflowConnectors`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as snowflake from "@pulumi/snowflake";
 *
 * // simple usage
 * const simple = snowflake.getOpenflowConnectors({});
 * // filtering (like)
 * const like = snowflake.getOpenflowConnectors({
 *     like: "connector-name",
 * });
 * // filtering (in)
 * const _in = snowflake.getOpenflowConnectors({
 *     "in": {
 *         schema: "<database_name>.<schema_name>",
 *     },
 * });
 * // filtering (starts_with)
 * const startsWith = snowflake.getOpenflowConnectors({
 *     startsWith: "connector-",
 * });
 * // filtering (limit)
 * const limit = snowflake.getOpenflowConnectors({
 *     limit: {
 *         rows: 10,
 *         from: "connector-name",
 *     },
 * });
 * // without additional data (to limit the number of calls make sure to set all of these to false)
 * const onlyShow = snowflake.getOpenflowConnectors({
 *     withDescribe: false,
 * });
 * ```
 *
 * > **Note** If a field has a default value, it is shown next to the type in the schema.
 */
export function getOpenflowConnectors(args?: GetOpenflowConnectorsArgs, opts?: pulumi.InvokeOptions): Promise<GetOpenflowConnectorsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("snowflake:index/getOpenflowConnectors:getOpenflowConnectors", {
        "in": args.in,
        "like": args.like,
        "limit": args.limit,
        "startsWith": args.startsWith,
        "withDescribe": args.withDescribe,
    }, opts);
}

/**
 * A collection of arguments for invoking getOpenflowConnectors.
 */
export interface GetOpenflowConnectorsArgs {
    /**
     * IN clause to filter the list of objects
     */
    in?: inputs.GetOpenflowConnectorsIn;
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    like?: string;
    /**
     * Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `startsWith` or `like`.
     */
    limit?: inputs.GetOpenflowConnectorsLimit;
    /**
     * Filters the output with **case-sensitive** characters indicating the beginning of the object name.
     */
    startsWith?: string;
    /**
     * (Default: `true`) Runs DESC OPENFLOW CONNECTOR for each connector returned by SHOW OPENFLOW CONNECTORS. The output of describe is saved to the description field. By default this value is set to true.
     */
    withDescribe?: boolean;
}

/**
 * A collection of values returned by getOpenflowConnectors.
 */
export interface GetOpenflowConnectorsResult {
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * IN clause to filter the list of objects
     */
    readonly in?: outputs.GetOpenflowConnectorsIn;
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    readonly like?: string;
    /**
     * Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `startsWith` or `like`.
     */
    readonly limit?: outputs.GetOpenflowConnectorsLimit;
    /**
     * Holds the aggregated output of all Openflow connector details queries.
     */
    readonly openflowConnectors: outputs.GetOpenflowConnectorsOpenflowConnector[];
    /**
     * Filters the output with **case-sensitive** characters indicating the beginning of the object name.
     */
    readonly startsWith?: string;
    /**
     * (Default: `true`) Runs DESC OPENFLOW CONNECTOR for each connector returned by SHOW OPENFLOW CONNECTORS. The output of describe is saved to the description field. By default this value is set to true.
     */
    readonly withDescribe?: boolean;
}
/**
 * > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `previewFeaturesEnabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.
 *
 * Data source used to get details of filtered Openflow connectors. Filtering is aligned with the current possibilities for [SHOW OPENFLOW CONNECTORS](https://docs.snowflake.com/en/sql-reference/sql/show-openflow-connectors). The results of SHOW and DESCRIBE are encapsulated in one output collection `openflowConnectors`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as snowflake from "@pulumi/snowflake";
 *
 * // simple usage
 * const simple = snowflake.getOpenflowConnectors({});
 * // filtering (like)
 * const like = snowflake.getOpenflowConnectors({
 *     like: "connector-name",
 * });
 * // filtering (in)
 * const _in = snowflake.getOpenflowConnectors({
 *     "in": {
 *         schema: "<database_name>.<schema_name>",
 *     },
 * });
 * // filtering (starts_with)
 * const startsWith = snowflake.getOpenflowConnectors({
 *     startsWith: "connector-",
 * });
 * // filtering (limit)
 * const limit = snowflake.getOpenflowConnectors({
 *     limit: {
 *         rows: 10,
 *         from: "connector-name",
 *     },
 * });
 * // without additional data (to limit the number of calls make sure to set all of these to false)
 * const onlyShow = snowflake.getOpenflowConnectors({
 *     withDescribe: false,
 * });
 * ```
 *
 * > **Note** If a field has a default value, it is shown next to the type in the schema.
 */
export function getOpenflowConnectorsOutput(args?: GetOpenflowConnectorsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetOpenflowConnectorsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("snowflake:index/getOpenflowConnectors:getOpenflowConnectors", {
        "in": args.in,
        "like": args.like,
        "limit": args.limit,
        "startsWith": args.startsWith,
        "withDescribe": args.withDescribe,
    }, opts);
}

/**
 * A collection of arguments for invoking getOpenflowConnectors.
 */
export interface GetOpenflowConnectorsOutputArgs {
    /**
     * IN clause to filter the list of objects
     */
    in?: pulumi.Input<inputs.GetOpenflowConnectorsInArgs | undefined>;
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    like?: pulumi.Input<string | undefined>;
    /**
     * Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `startsWith` or `like`.
     */
    limit?: pulumi.Input<inputs.GetOpenflowConnectorsLimitArgs | undefined>;
    /**
     * Filters the output with **case-sensitive** characters indicating the beginning of the object name.
     */
    startsWith?: pulumi.Input<string | undefined>;
    /**
     * (Default: `true`) Runs DESC OPENFLOW CONNECTOR for each connector returned by SHOW OPENFLOW CONNECTORS. The output of describe is saved to the description field. By default this value is set to true.
     */
    withDescribe?: pulumi.Input<boolean | undefined>;
}

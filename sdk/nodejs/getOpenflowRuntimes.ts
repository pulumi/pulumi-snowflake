// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `previewFeaturesEnabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.
 *
 * Data source used to get details of filtered Openflow runtimes. The results of SHOW and DESCRIBE are encapsulated in one output collection `openflowRuntimes`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as snowflake from "@pulumi/snowflake";
 *
 * // simple usage
 * const simple = snowflake.getOpenflowRuntimes({});
 * // filtering (like)
 * const like = snowflake.getOpenflowRuntimes({
 *     like: "runtime-name",
 * });
 * // filtering (in)
 * const _in = snowflake.getOpenflowRuntimes({
 *     "in": {
 *         schema: "<database_name>.<schema_name>",
 *     },
 * });
 * // filtering (starts_with)
 * const startsWith = snowflake.getOpenflowRuntimes({
 *     startsWith: "runtime-",
 * });
 * // filtering (limit)
 * const limit = snowflake.getOpenflowRuntimes({
 *     limit: {
 *         rows: 10,
 *         from: "runtime-name",
 *     },
 * });
 * // without additional data (to limit the number of calls make sure to set all of these to false)
 * const onlyShow = snowflake.getOpenflowRuntimes({
 *     withDescribe: false,
 * });
 * ```
 *
 * > **Note** If a field has a default value, it is shown next to the type in the schema.
 */
export function getOpenflowRuntimes(args?: GetOpenflowRuntimesArgs, opts?: pulumi.InvokeOptions): Promise<GetOpenflowRuntimesResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("snowflake:index/getOpenflowRuntimes:getOpenflowRuntimes", {
        "in": args.in,
        "like": args.like,
        "limit": args.limit,
        "startsWith": args.startsWith,
        "withDescribe": args.withDescribe,
    }, opts);
}

/**
 * A collection of arguments for invoking getOpenflowRuntimes.
 */
export interface GetOpenflowRuntimesArgs {
    /**
     * IN clause to filter the list of objects
     */
    in?: inputs.GetOpenflowRuntimesIn;
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    like?: string;
    /**
     * Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `startsWith` or `like`.
     */
    limit?: inputs.GetOpenflowRuntimesLimit;
    /**
     * Filters the output with **case-sensitive** characters indicating the beginning of the object name.
     */
    startsWith?: string;
    /**
     * (Default: `true`) Runs DESC OPENFLOW RUNTIME for each runtime returned by SHOW OPENFLOW RUNTIMES. The output of describe is saved to the description field. By default this value is set to true.
     */
    withDescribe?: boolean;
}

/**
 * A collection of values returned by getOpenflowRuntimes.
 */
export interface GetOpenflowRuntimesResult {
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * IN clause to filter the list of objects
     */
    readonly in?: outputs.GetOpenflowRuntimesIn;
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    readonly like?: string;
    /**
     * Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `startsWith` or `like`.
     */
    readonly limit?: outputs.GetOpenflowRuntimesLimit;
    /**
     * Holds the aggregated output of all Openflow runtime details queries.
     */
    readonly openflowRuntimes: outputs.GetOpenflowRuntimesOpenflowRuntime[];
    /**
     * Filters the output with **case-sensitive** characters indicating the beginning of the object name.
     */
    readonly startsWith?: string;
    /**
     * (Default: `true`) Runs DESC OPENFLOW RUNTIME for each runtime returned by SHOW OPENFLOW RUNTIMES. The output of describe is saved to the description field. By default this value is set to true.
     */
    readonly withDescribe?: boolean;
}
/**
 * > **Caution: Preview Feature** This feature is considered a preview feature in the provider, regardless of the state of the resource in Snowflake. We do not guarantee its stability. It will be reworked and marked as a stable feature in future releases. Breaking changes are expected, even without bumping the major version. To use this feature, add the relevant feature name to `previewFeaturesEnabled` field in the provider configuration. Please always refer to the Getting Help section in our Github repo to best determine how to get help for your questions.
 *
 * Data source used to get details of filtered Openflow runtimes. The results of SHOW and DESCRIBE are encapsulated in one output collection `openflowRuntimes`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as snowflake from "@pulumi/snowflake";
 *
 * // simple usage
 * const simple = snowflake.getOpenflowRuntimes({});
 * // filtering (like)
 * const like = snowflake.getOpenflowRuntimes({
 *     like: "runtime-name",
 * });
 * // filtering (in)
 * const _in = snowflake.getOpenflowRuntimes({
 *     "in": {
 *         schema: "<database_name>.<schema_name>",
 *     },
 * });
 * // filtering (starts_with)
 * const startsWith = snowflake.getOpenflowRuntimes({
 *     startsWith: "runtime-",
 * });
 * // filtering (limit)
 * const limit = snowflake.getOpenflowRuntimes({
 *     limit: {
 *         rows: 10,
 *         from: "runtime-name",
 *     },
 * });
 * // without additional data (to limit the number of calls make sure to set all of these to false)
 * const onlyShow = snowflake.getOpenflowRuntimes({
 *     withDescribe: false,
 * });
 * ```
 *
 * > **Note** If a field has a default value, it is shown next to the type in the schema.
 */
export function getOpenflowRuntimesOutput(args?: GetOpenflowRuntimesOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetOpenflowRuntimesResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("snowflake:index/getOpenflowRuntimes:getOpenflowRuntimes", {
        "in": args.in,
        "like": args.like,
        "limit": args.limit,
        "startsWith": args.startsWith,
        "withDescribe": args.withDescribe,
    }, opts);
}

/**
 * A collection of arguments for invoking getOpenflowRuntimes.
 */
export interface GetOpenflowRuntimesOutputArgs {
    /**
     * IN clause to filter the list of objects
     */
    in?: pulumi.Input<inputs.GetOpenflowRuntimesInArgs | undefined>;
    /**
     * Filters the output with **case-insensitive** pattern, with support for SQL wildcard characters (`%` and `_`).
     */
    like?: pulumi.Input<string | undefined>;
    /**
     * Limits the number of rows returned. If the `limit.from` is set, then the limit will start from the first element matched by the expression. The expression is only used to match with the first element, later on the elements are not matched by the prefix, but you can enforce a certain pattern with `startsWith` or `like`.
     */
    limit?: pulumi.Input<inputs.GetOpenflowRuntimesLimitArgs | undefined>;
    /**
     * Filters the output with **case-sensitive** characters indicating the beginning of the object name.
     */
    startsWith?: pulumi.Input<string | undefined>;
    /**
     * (Default: `true`) Runs DESC OPENFLOW RUNTIME for each runtime returned by SHOW OPENFLOW RUNTIMES. The output of describe is saved to the description field. By default this value is set to true.
     */
    withDescribe?: pulumi.Input<boolean | undefined>;
}
